# v0.6 真实代码地图

目的：记录现有代码库的真实入口，避免后续实现者重写已有设施。仅记录已读确认的符号与行号。

## 1. Provider request entry

- 基类：`src/llmtrace/providers/base.py:95` `BaseProvider(ABC)`；预算/证据钩子在 `__init__`（`:107`）注入。
- 入口签名：`base.py:228` `async def complete(self, model: str, messages: list[dict[str, str]], *, options: CompletionOptions | None = None) -> HTTPEvidence`；另见 `list_models()` `:187`、`stream_complete()` `:292`。
- 协议实现：`openai_compatible.py:19` `OpenAICompatibleProvider._apply_options_to_body` `:41`；`anthropic_compatible.py:22` `AnthropicCompatibleProvider._apply_options_to_body` `:71`。
- `CompletionOptions` 真实位置是 `src/llmtrace/benchmarks/models.py:753`（**不在** `models/`）；字段 `until/stop/temperature/max_gen_toks/max_tokens/do_sample` `:770-775`，冲突消解 `base.py:69 _resolve_max_tokens`。
- owns：请求构造 + 协议字段映射 + 每次真实请求前 `_consume_budget()`（`:126`）+ 请求后 `_record_evidence()`（`:121`）；不负责证据存储。

## 2. Evidence entry

- `EvidenceType` 定义：`src/llmtrace/models/evidence.py:13`（StrEnum），成员 `:16-21`：`MODEL_CATALOG, BASELINE, INVALID_MODEL, STREAMING_BASELINE, STREAMING_COMPARISON, CONNECTIVITY`。
- `HTTPEvidence` 模型：`models/evidence.py:24`；注意 `evidence_type` 字段类型是 `str | None`（`:29`），并**未**强类型为 `EvidenceType`（base.py:191 直接用字符串字面量 "model_catalog"）。
- 记录契约：`src/llmtrace/execution/evidence.py:18` `EvidenceRecorder(Protocol).record(evidence)` `:21`。
- 实现：`evidence.py:26 InMemoryEvidenceRecorder`；重复 id 在 `record()` `:39-46` 抛 `DuplicateEvidenceError`。
- 「exactly once」= Provider 侧每个真实请求调用一次 `_record_evidence`（`base.py:225 / :289 / :363`）+ Recorder 侧 fail-closed 去重（`evidence.py:41`）；无单一开关。
- Runner 持有单例：`execution/runner.py:162 self._recorder = InMemoryEvidenceRecorder()`。

## 3. Budget entry

- `src/llmtrace/execution/budget.py:12 RequestBudget`；API 仅 `consume(count=1)` `:40` 与只读属性 `maximum_requests/consumed_requests/remaining_requests` `:28-38`；超限抛 `RequestBudgetExceededError` `:6`。
- **无 `plan()` 方法**；dry-run 计划计数来自 `UnifiedExecutionPlan.maximum_requests`（`execution/models.py:93`），在 `planner.py:114 maximum_requests=planned_requests` 赋值。
- 消费调用点：`providers/base.py:126 _consume_budget()` → `base.py:129 self._request_budget.consume(1)`。
- Runner 用 plan 的上限构造：`execution/runner.py:255 budget = RequestBudget(plan.maximum_requests)`；CLI dry-run 展示 `plan.maximum_requests`：`cli.py:336`。
- owns：真实目标 HTTP 请求的硬上限（发送前 fail-closed），失败请求同样计数。

## 4. Unified runner entry

- `src/llmtrace/execution/runner.py:122 UnifiedAuditRunner`；入口 `run()` `:219` → `_execute()` `:242`；请求模型 `UnifiedRunRequest` `:106`。
- 构造签名 `:136-151`：`(config, *, api_key, target_id, repository, code_backend=None, compare_latest=True, baseline_snapshot_path=None, reference_snapshot_path=None, reference_set_path=None, max_wall_seconds=None, progress_sink=None, cancel_token=None)`。
- flow 顺序：preflight `:251` → plan `:254` → RequestBudget `:255` → create_provider `:258` → PROTOCOL `:272` → BENCHMARK/QuickSuiteRunner `:293-302` → SCORING/aggregate `:304-306` → SNAPSHOT `:308` → CALIBRATION `:332` → HISTORY `:380` → REFERENCE `:395` → `_write_artifacts` `:428`。
- 计划单一构造器：`execution/planner.py:49 build_unified_execution_plan(...)`；`_plan()` `runner.py:552`。
- 观测/取消：`execution/progress.py` `ProgressSink = Callable[[ProgressEvent], None]` `:101`、`CancellationToken` `:45`、`RunCancelledError` `:39`。

## 5. Artifact persistence entry

- `src/llmtrace/execution/artifacts.py:69 RunArtifactRepository`；写入入口 `commit(manifest, artifacts)` `:90`；staging + hash + 原子 rename `:114-135`，manifest 最后写 `:129`，已存在 id 抛 `DuplicateExecutionError` `:103`。
- Runner 写入器：`execution/runner.py:814 _write_artifacts()`；产物键 `report.json`/`report.html` `:861-862`、`capability_profile.json` `:865`、`behavior_snapshot.json` `:867`、`benchmark_runs.json` `:869`（helper `_benchmark_runs_json` `:927`）；`self._repository.commit(...)` `:901`。
- manifest 模型：`execution/models.py:162 RunArtifactManifest`（`MANIFEST_VERSION="0.3.0"` `:159`）。
- 报告序列化器：`reporting/json_report.py:220 generate_json_report`、`reporting/html_report.py:160 generate_html_report`（先 scrub 再算 content_hash，见 json_report `:315-327`）。
- 数据目录布局：`appdir.py:32 AppLayout`、`appdir.py:23 default_data_root`（`$LLMTRACE_HOME` 或 `~/.llmtrace`）。

## 6. CLI result entry

- `run` 命令：`src/llmtrace/cli.py:224` def run(...) → 构造 `UnifiedAuditRunner` `:392` → `asyncio.run(runner.run())` `:420` → `print_unified_summary` `:442`。
- 结果渲染：`reporting/console.py:241 print_unified_summary(result, artifacts)`（置信度/路由/claimed gap/工件路径）。
- 旧审计入口：`cli.py:115` def audit(...) → `print_audit_summary` `console.py:46`；比较 `cli.py:542 compare`。
- 工件路径映射（CLI 展示用）：`cli.py:437-441`（`output_dir/runs/<execution_id>/...`）；dry-run `print_dry_run` `console.py:201`。
- CLI 进度 sink：`console.py:397 CliProgress`（实现 `ProgressSink.on_event` `:459`）。

## 7. Reference repository patterns

- canonical JSON + SHA-256 self-checksum：`reference/reference_set.py:73 _compute_content_sha256`（`content_sha256=""` → `json.dumps(sort_keys=True, separators=(",", ":"), ensure_ascii=True)` → `sha256_of`）；模型封装 `ReferenceSet` `:154`、`compute_content_sha256()` `:213`、`verify_content_hash()` `:217`；UTC 规范化 `_json_safe` `:58`。
- append-only 存储：`reference/repository.py:38 ReferenceSetRepository`；`save()` `:52`（先磁盘后内存）、独占创建 `_write_file` `open("x")` `:181-199`、`set_sha256()` `:109`、`verify()` `:113`。
- 可信快照锚（sidecar）模式：`scoring/reference.py:329 ReferenceRepository.save_trusted`、`_write_trusted_files` `:538`、锚点模型 `ReferenceSnapshotManifest` `:214`、`verify_trusted_snapshot()` `:445`。
- suite content hashing 现有模式：`adapters/quick_suite.py:303 get_quick_suite_content_sha256`（基于 manifest 的 canonical payload → SHA-256，`verify_quick_suite_resources` 前置校验 `:227`）；generation config 同法 `:183 get_quick_suite_generation_config_sha256`；single-item subset `_subset_sha256` `:222`。
- 哈希工具真实符号：`utilities/hashing.py:10 sha256_hash`、`:17 stable_json_hash`；注意 `stable_json_hash` 用 `ensure_ascii=False` `:19`，与 reference/suite 模式的 `ensure_ascii=True` 不一致（复用需显式选择）。
- 兼容性门禁可复用：`reference/validation.py:128 validate_reference_set_for_calibration`（sidecar/快照 SHA/identity/清单 SHA 链）+ `:388 _assert_compatible`。
