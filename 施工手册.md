# LLMTrace — Usable MVP Product Sprint
# 从“评测后端”直接推进到“普通用户真正能使用的本地产品”

你现在继续开发 **LLMTrace / 模型寻迹**。

本轮的最高目标发生重大变化。

以前我们的开发方式过于偏向：

```text
先把一个底层模块做完
→ 审查
→ 加门禁
→ 再做下一个底层模块
→ 再审查
→ 再补基础设施
```

这导致项目虽然底层越来越严谨，但开发周期已经明显过长，普通用户仍然没有一个真正能够打开、填写 API、点击开始并得到完整结果的产品。

从现在开始，停止这种开发节奏。

---

# 一、本轮最高目标

这次不是：

> 再完成一个 Foundation。

也不是：

> 再完成一个 Scoring / Fingerprint / Service 子模块。

而是：

> **直接做出 LLMTrace 第一版真正可以使用的完整本地产品。**

你可以把以前的开发方式理解成：

> 一直在修小区大门。

这次要做的是：

> **先把整个小区建设出来，让人能够真正入住。**

楼道灯以后再换。

墙以后再刷。

绿化以后再优化。

但现在：

```text
必须有房子
必须有道路
必须有水电
必须能进去
必须能住
```

对应 LLMTrace：

```text
必须能启动程序
必须能输入 API
必须能估算测试
必须能点击开始
必须能看到进度
必须能完成测试
必须能看到结果
必须有历史记录
必须能打开详细报告
```

只要核心语义正确，局部实现第一版可以简单。

---

# 二、不要再按旧 Roadmap 串行开发

旧路线大致是：

```text
v0.4 Calibration
→ v0.5 Fingerprint + Routing
→ v0.6 Product Service + Web
```

本轮不再遵循这种串行方式。

直接合并为：

# **LLMTrace Usable MVP**

即：

```text
现有评测核心
+
Calibration
+
基础 Confidence
+
基础 Similarity / Routing 判断
+
Service Layer
+
SQLite History
+
Local Web UI
+
Progress
+
Result Dashboard
+
Export
```

一次打通。

不要求每个模块达到未来正式版质量。

要求：

> **所有主要产品区域先出现，并且主流程真的能跑。**

---

# 三、当前真实基线

仓库：

```text
MortonCheung/LLMTrace
```

当前正在开发的基础分支：

```text
feat/v0.4-b-formal-calibration
```

当前 HEAD 应为：

```text
844bcb6b61c38f00f87a48c7ee5e8e532b124149
```

当前 PR：

```text
#19
feat: add v0.4-B formal reference calibration
```

PR #19 当前尚未合并 main。

它已经完成：

```text
ReferenceSet
→ Reference Calibration
→ calibrated dimension score
→ calibrated total score
→ Claimed Model Gap
```

并且：

```text
llmtrace run --reference-set
```

已经能够进入 Calibration。

本轮不要重新做这些。

直接把：

```text
feat/v0.4-b-formal-calibration
```

作为新的实际开发基线。

---

# 四、Git 策略

不要为了这次开发停下来做 PR #19 的长时间 Owner Review。

也不要修改 main。

从：

```text
feat/v0.4-b-formal-calibration
```

当前 HEAD：

```text
844bcb6
```

创建新分支：

```text
feat/usable-local-mvp
```

或者：

```text
feat/v0.5-usable-mvp
```

二选一。

这样形成 stacked development：

```text
main
  ↓
v0.4-B
  ↓
Usable MVP
```

本轮完成后再统一处理合并顺序。

---

# 五、必须继续使用 Ponytail 思路

Ponytail 继续有效。

但这里尤其要注意：

Ponytail 不等于：

> 尽量少做功能。

真正原则是：

> **先读懂现有链路，优先利用已有东西，把产品用最短路径接起来。**

例如现在已经有：

```text
UnifiedAuditRunner
RunPlan
Quick Suite
CapabilityProfile
ReferenceSet
Calibration
Behavior Drift
RunArtifactRepository
JSON Report
HTML Report
Console Report
```

因此：

不要再创建：

```text
NewRunner
WebRunner
ServiceRunner
ProductRunner
```

应该：

```text
Web / Service
      ↓
直接调用 UnifiedAuditRunner
```

同理：

历史记录不要重新发明 Artifact。

SQLite 只保存：

```text
索引
状态
摘要
路径
```

真正的详细审计事实继续由：

```text
RunArtifactRepository
```

负责。

---

# 六、最终用户体验

这轮结束以后，一个普通用户应该可以：

```bash
pip install -e .
llmtrace web
```

然后浏览器打开：

```text
http://127.0.0.1:8765
```

看到 LLMTrace。

---

# 七、首页：开始测试

页面第一屏就是：

# LLMTrace
### 第三方 AI API 黑盒验货

表单：

```text
Base URL
API Key
Protocol
Claimed Model
Reasoning Effort
Temperature
Max Output Tokens
Reference Set
Budget
Pricing
```

Protocol：

```text
OpenAI Compatible
Anthropic Compatible
```

第一版不用做复杂自动识别。

可以：

```text
默认 Auto
Auto 失败后让用户选择
```

如果现有 Provider 已经能检测，直接复用。

---

# 八、API Key 绝不能持久化

Web 用户输入：

```text
API Key
```

必须遵守：

```text
浏览器
→ POST localhost
→ 仅进程内存
→ run 完成 / cancel 后删除
```

禁止写入：

```text
SQLite
JSON
HTML
logs
RunArtifact
```

SQLite 只保存：

```text
has_api_key = true
```

甚至可以不保存这个字段。

---

# 九、测试档位

不要为了设计三套新 benchmark 再拖几周。

第一版真正可用的测试档位：

### Quick

直接使用当前：

```text
Quick Suite
32 items
4 dimensions
```

这是默认模式。

### Standard

如果当前架构能够低成本支持：

使用：

```text
Quick Suite
+
关键题重复采样
```

用于提高：

```text
stability
confidence
routing signal
```

如果实现需要大改 benchmark：

本轮不要硬做。

UI 可以先显示：

```text
Standard
Coming next
```

但 **Quick 必须完全可用**。

### Deep

同理。

不要伪造一个实际上只是 Quick × 3 的“深度测试”，然后把它宣传成不同 benchmark。

---

# 十、估算步骤

用户填写完配置后：

点击：

# 估算测试

直接复用现有：

```text
dry-run
RunPlan
Budget
```

输出：

```text
预计请求数
预计最大输出 Token
预计运行时间
预计费用
Suite
ReferenceSet
是否运行代码
```

---

# 十一、费用第一版不要做价格数据库

不要为了模型价格再创建：

```text
ModelPricingRegistry
DynamicPricingUpdater
ProviderPricingCrawler
```

第一版直接允许用户填写：

```text
Input price / 1M tokens
Output price / 1M tokens
```

都是可选。

如果填写：

```text
系统计算预计费用
```

如果没填写：

```text
Estimated cost: unavailable
```

正式结果也按实际 Token 计算实际估算费用。

未来再做价格数据库。

---

# 十二、开始测试

点击：

# Start Audit

后端调用：

```text
RunService
      ↓
UnifiedAuditRunner
```

不要复制 runner。

---

# 十三、新增一个很薄的 Service Layer

PRD 原本设计：

```python
create_run(config)
estimate_run(run_id)
start_run(run_id)
get_progress(run_id)
cancel_run(run_id)
get_result(run_id)
```

本轮实现这个思想。

可以设计类似：

```text
src/llmtrace/service/
    models.py
    runs.py
```

或根据真实代码决定最合理位置。

不要为了形式完全照抄目录。

核心对象：

```python
RunService
```

职责：

```text
create
estimate
start
progress
cancel
result
history
```

它本身不重新实现 audit。

---

# 十四、执行模型：单进程即可

首版：

```text
FastAPI
+
asyncio task
+
UnifiedAuditRunner
```

不要：

```text
Celery
Redis
RabbitMQ
Kafka
worker cluster
```

Web Server 进程内部维护：

```text
run_id → asyncio.Task
```

够了。

服务器重启导致正在运行的任务丢失：

首版允许。

历史结果仍保存在磁盘和 SQLite。

---

# 十五、FastAPI

加入最小依赖：

```text
fastapi
uvicorn
```

继续使用项目已有：

```text
jinja2
```

不要引入 React/Vite/Node。

原因：

> 现在目标是最快做出可用产品，而不是搭第二套前端工程体系。

---

# 十六、为什么不用 React

第一版 Web：

```text
FastAPI
+
Jinja2
+
Vanilla JS
+
CSS
```

足够。

以后如果产品确定继续做：

再换 React。

现在不要增加：

```text
npm
vite
eslint
typescript
frontend build pipeline
```

这些都属于“装修”。

---

# 十七、Web API

实现最小接口：

```text
POST /api/runs
POST /api/runs/{id}/estimate
POST /api/runs/{id}/start

GET  /api/runs/{id}
GET  /api/runs/{id}/events

POST /api/runs/{id}/cancel

GET /api/runs
GET /api/runs/{id}/result

GET /api/reference-sets
```

不需要做：

```text
GraphQL
OpenAPI SDK
API version negotiation
```

---

# 十八、SSE 实时进度

实现：

```text
GET /api/runs/{id}/events
```

使用：

```text
Server-Sent Events
```

事件示例：

```json
{
  "type": "progress",
  "stage": "benchmark",
  "completed": 18,
  "total": 32,
  "message": "Running capability benchmark"
}
```

阶段至少包括：

```text
preflight
protocol
benchmark
scoring
calibration
comparison
reporting
completed
```

---

# 十九、不要为了 Progress 重写 Runner

如果 UnifiedAuditRunner 当前没有 progress callback：

最小增加：

```python
ProgressSink
```

或者：

```python
Callable[[ProgressEvent], None]
```

默认：

```text
None
```

CLI 完全不受影响。

在现有阶段边界发送事件。

不要为了精确到每一个内部函数而大改 runner。

---

# 二十、Cancellation

第一版实现 cooperative cancellation。

用户点击：

# Cancel

系统：

```text
设置 cancellation flag
```

runner 在：

```text
每个 Probe 前
每个 Benchmark item 前
主要 stage 切换前
```

检查。

如果已有 cancellation abstraction：

直接复用。

如果没有：

加一个非常薄的：

```python
CancellationToken
```

已经发出的单个 HTTP request：

第一版不要求瞬间强杀。

但：

```text
当前请求结束后必须停止继续请求
```

---

# 二十一、SQLite

实现：

```text
~/.llmtrace/llmtrace.db
```

或者项目已有 data root。

使用 Python 标准库：

```python
sqlite3
```

不要引入 ORM。

不要 SQLAlchemy。

---

# 二十二、SQLite 只做索引

表可以非常简单：

```text
runs
```

字段类似：

```text
run_id
created_at
updated_at

base_url_redacted
protocol
claimed_model

status
stage

reference_set_id

capability_score
risk_level
claimed_gap

request_count
input_tokens
output_tokens
duration_ms

artifact_path
report_json_path
report_html_path

error_summary
```

API Key：

绝不进入 DB。

---

# 二十三、历史页面

Web 首页之外增加：

# History

显示：

```text
时间
模型
Endpoint
综合分
Gap
协议风险
状态
耗时
```

点击进入：

```text
/run/{run_id}
```

查看结果。

---

# 二十四、首页 UI

不要做传统企业后台侧边栏。

也不要做炫技动画。

风格：

```text
Linear
GitHub
Vercel
OpenAI Dashboard
```

特征：

```text
白 / 深灰
高信息密度
清晰边界
少量强调色
大量留白
```

页面核心就是：

```text
输入 → 测试 → 结果
```

不要：

```text
three.js
WebGL
GSAP
粒子
玻璃拟态
巨型渐变
```

这个项目的价值在数据。

---

# 二十五、页面 1：New Audit

布局：

```text
┌─────────────────────────────────────┐
│ LLMTrace                            │
│ Third-party AI API audit            │
├─────────────────────────────────────┤
│ Endpoint                            │
│ Base URL                            │
│ API Key                             │
│ Protocol                            │
│ Claimed Model                       │
│                                     │
│ Test Configuration                  │
│ Quick                               │
│ Reasoning Effort                    │
│ Max Output Tokens                   │
│ Reference Set                       │
│                                     │
│ Cost                                │
│ Input price / 1M                    │
│ Output price / 1M                   │
│                                     │
│ [ Estimate ]     [ Start Audit ]    │
└─────────────────────────────────────┘
```

---

# 二十六、页面 2：Running

用户点击 Start 后：

不要跳到空页面。

显示：

```text
Running audit...

Protocol check      ✓
Benchmark           18 / 32
Scoring             waiting
Calibration         waiting
Report              waiting
```

下面：

```text
Requests       23 / ~38
Elapsed        02:41
Input Tokens   18.4k
Output Tokens  7.1k
```

按钮：

```text
Cancel
```

SSE 更新。

---

# 二十七、页面 3：Result

这是整个产品最重要的页面。

顶部：

```text
LLMTrace Audit Result
```

核心 Hero：

```text
Capability
78.4 / 100
```

旁边：

```text
Confidence
Medium / High
```

如果没有 Calibration：

```text
Formal score unavailable

Raw capability measurement completed
```

绝不伪造 0–100。

---

# 二十八、四维能力

显示：

```text
Reasoning                81.2
Coding                   74.6
Math & Science           79.8
Instruction Following    76.1
```

用简单 bar。

不需要复杂图表库。

纯 CSS 即可。

---

# 二十九、声明模型差距

如果存在可信匹配：

```text
Claimed Model

gpt-x

Trusted reference
86.7

Measured
78.4

Gap
-8.3
```

解释：

```text
The tested endpoint performed below the compatible
trusted reference configuration.
```

然后固定免责声明：

```text
Capability difference does not prove upstream model identity.
```

---

# 三十、协议风险

直接利用现有：

```text
Protocol Audit
Risk Analysis
Findings
Evidence
```

结果页显示：

```text
Protocol Risk
LOW / MEDIUM / HIGH / INCONCLUSIVE
```

然后：

```text
Model identifier
Streaming
Metadata consistency
Stability
```

---

# 三十一、速度

输出：

```text
Average latency
P50
P95
Total duration
```

如果已有 Evidence 足够：

直接计算。

不要新建 observability framework。

---

# 三十二、Token

输出：

```text
Input Tokens
Output Tokens
Total Tokens
```

如果 Provider 没返回：

显示：

```text
Unavailable
```

不要估造。

---

# 三十三、费用

如果用户填写了 pricing：

```text
Estimated actual cost
$0.42
```

否则：

```text
Cost unavailable
```

---

# 三十四、Confidence 第一版

现在不要继续研究一个月的统计体系。

先实现：

```text
High
Medium
Low
Unavailable
```

依据：

```text
item coverage
provider failure rate
graded item count
reference calibration available
reference identity count
repeat stability（如果存在）
```

将规则放到：

```text
ConfidencePolicy v1
```

但保持简单。

---

# 三十五、Confidence 示例

例如：

### High

```text
32/32 graded
0 provider failures
valid calibration
>= 5 reference identities
```

### Medium

```text
大部分 item 完成
或 reference coverage 较弱
```

### Low

```text
明显 failure
或数据不足
```

### Unavailable

```text
无法形成可信 capability measurement
```

---

# 三十六、95% CI 怎么处理

PRD 最终要求 CI。

但这次不要让 CI 阻止产品上线。

如果现有 item-level score 很容易 bootstrap：

可以实现简单 deterministic bootstrap。

例如：

```text
seed 固定
1000 resamples
```

输出：

```text
Approx. 95% CI
```

并明确：

```text
experimental
```

如果实现需要破坏 scoring：

本轮不做。

只输出：

```text
Confidence Level
```

CI 留下一阶段。

**不要因为 CI 没做好而停止整个 MVP。**

---

# 三十七、行为相似度：第一版必须出现，但可以简单

PRD 要：

```text
Top-K behavior similarity
```

这次先做第一版。

不要上复杂：

```text
LLMmap
MMD
Model Equality Testing
embedding model
```

先利用已经存在的数据。

---

# 三十八、行为特征第一版

优先从现有 Artifact 提取：

```text
dimension raw scores
item graded / failed pattern
item correctness / partial score
response token count
latency
instruction failure pattern
provider metadata stability
```

构造：

```python
BehaviorFeatureVector
```

---

# 三十九、Similarity 第一版

对于拥有完整可比较 source artifact 的 Reference Identity：

计算简单距离。

推荐：

```text
normalized weighted L1 distance
```

或：

```text
cosine similarity
```

根据当前数据最自然的方式选择。

不要引入 ML 模型。

输出：

```text
Behavior Similarity

Model A       82%
Model B       71%
Model C       64%
```

必须固定：

```text
Behavioral similarity only.
Not model identity verification.
```

---

# 四十、Similarity Fail Closed

如果：

```text
ReferenceSet 没有足够 item-level data
```

显示：

```text
Behavior similarity unavailable
```

不要因为名字匹配就伪造 similarity。

---

# 四十一、Routing 第一版

不要尝试直接推断：

```text
70% GPT-X + 30% Model-Y
```

第一版只输出：

```text
Routing Stability
```

等级：

```text
Stable
Mostly Stable
Suspicious
Insufficient Data
```

依据现有：

```text
model identifier variation
provider failure
latency clusters
token distribution
behavior drift
repeat samples（如果存在）
```

---

# 四十二、Routing 文案

允许：

```text
Request behavior is inconsistent across samples.
Mixed routing is possible.
```

禁止：

```text
30% of requests used Claude.
```

除非未来真的有充分统计证据。

---

# 四十三、已经存在 Behavior Drift，必须展示

当前项目已经有：

```text
Behavior Drift Foundation
```

不要让它继续只躺在 JSON。

如果存在兼容历史运行：

结果页增加：

# Compared with previous run

显示：

```text
Overall drift
Reasoning drift
Coding drift
Math drift
Instruction drift
Reliability change
```

这已经是很实际的产品能力。

---

# 四十四、历史对比

History 页面：

用户点两个兼容运行：

可以先不做复杂手动选择。

第一版：

```text
当前 run
vs
latest compatible run
```

即可。

现有 runner 已经做这个。

前端只展示。

---

# 四十五、Reference Sets

新增：

```text
GET /api/reference-sets
```

扫描本地：

```text
references/sets
```

以及配置路径。

Web 下拉显示：

```text
Reference Set Name
Version
Member count
Suite
Calibration compatible
```

---

# 四十六、没有 ReferenceSet 也必须能用

这是非常重要的产品原则。

用户第一次启动 LLMTrace：

可能根本没有 reference data。

他仍然应该能：

```text
Protocol audit
Quick Suite
Raw dimension measurement
risk
latency
tokens
report
history
```

只是：

```text
Formal 0–100 = unavailable
Claimed gap = unavailable
Behavior similarity = unavailable
```

页面解释原因。

不能整个应用因为没 ReferenceSet 就废掉。

---

# 四十七、Reference Capture 暂时继续 CLI

当前已经有：

```text
llmtrace reference capture
llmtrace reference set-create
```

本轮不需要再给它做 Web UI。

未来再做。

Web 只负责：

```text
读取 / 选择已有 ReferenceSet
```

---

# 四十八、导出

结果页：

```text
Download JSON
Open HTML Report
```

直接使用已有：

```text
report.json
report.html
```

不要生成第二套 report。

---

# 四十九、专业详情

结果页底部：

# Technical Details

折叠显示：

```text
Suite
Suite version
Suite content SHA

Scoring policy
Calibration policy
ReferenceSet
Adapter version

Requests
Evidence IDs

Raw scores
```

专业用户能复核。

---

# 五十、CLI 必须继续工作

本轮不能因为 Web 破坏：

```text
llmtrace run
llmtrace audit
llmtrace inspect
llmtrace compare
llmtrace reference ...
```

Web 是：

```text
新的调用层
```

不是替代。

---

# 五十一、新 CLI

新增：

```bash
llmtrace web
```

选项：

```text
--host
--port
--data-dir
```

默认：

```text
host = 127.0.0.1
port = 8765
```

启动后输出：

```text
LLMTrace Web
http://127.0.0.1:8765
```

---

# 五十二、安全默认

默认绝不绑定：

```text
0.0.0.0
```

因为页面会接收 API Key。

只有用户明确：

```bash
--host 0.0.0.0
```

才允许。

并输出 warning。

---

# 五十三、不要做账户

本地 MVP：

```text
无登录
无注册
无多用户
无权限系统
```

---

# 五十四、不要做部署平台

本轮不做：

```text
Docker Cloud
Kubernetes
Cloudflare
AWS
公网 SaaS
```

本轮目标：

# Local-first application

---

# 五十五、不要做动态网络更新

PRD 里的：

```text
Source Registry
Live updater
License crawler
```

全部以后做。

不要让它阻挡 MVP。

---

# 五十六、不要增加新 Benchmark

现有 Quick Suite 足够支撑第一版产品。

不要这轮又开始：

```text
LiveBench
EvalPlus
Inspect AI
```

全部不做。

以后装修。

---

# 五十七、这一轮允许“实验性”

以下能力：

```text
Confidence
Behavior Similarity
Routing Stability
```

可以在 UI 标：

```text
Experimental
```

这比继续半年不交付产品更好。

但：

```text
Formal Capability Score
Claimed Model Gap
Raw Evidence
```

不能假。

---

# 五十八、API 错误体验

例如：

```text
401
```

页面不要：

```text
Internal Server Error
```

而要：

```text
Authentication failed.
Check the API key and endpoint.
```

---

# 五十九、常见错误必须映射

至少：

```text
Invalid URL
Authentication failure
Model not found
Rate limit
Timeout
Provider unavailable
Budget exceeded
Cancelled
ReferenceSet invalid
Calibration unavailable
```

---

# 六十、错误不能泄露 key

检查所有：

```text
exception
repr
traceback
logger
JSON response
```

不得包含：

```text
Authorization
x-api-key
API key value
```

---

# 六十一、Demo / Mock 模式

当前项目已有 Mock Provider。

新增方便启动的：

```bash
llmtrace web --demo
```

或者：

```bash
llmtrace demo
```

二选一。

Demo 模式：

```text
自动连接项目 Mock Server
```

让没有 API Key 的用户也可以体验：

```text
创建 run
→ progress
→ capability
→ report
→ history
```

---

# 六十二、Demo Calibration 不得冒充正式 Reference

如果使用 test fixture：

页面必须显示：

```text
DEMO
```

不能显示：

```text
Trusted Calibration
```

如果当前 integrity policy 禁止 fixture calibration：

保持禁止。

Demo 可以只展示：

```text
raw capability
```

没有必要为了漂亮数字破坏安全语义。

---

# 六十三、端到端验收

这次最终验收不是：

```text
pytest passed
```

而是：

## Scenario A — Mock

用户：

```bash
llmtrace web
```

打开页面。

输入 Mock Provider。

点击：

```text
Estimate
Start
```

看到：

```text
progress
completed
result
history
report
```

完整成功。

---

# 六十四、Scenario B — 无 ReferenceSet

运行真实或 Mock endpoint。

必须：

```text
测试成功
```

结果：

```text
raw capability available
formal calibration unavailable
```

页面不能坏。

---

# 六十五、Scenario C — 有 ReferenceSet

使用测试中构造的可信 fixture trust chain。

必须得到：

```text
0–100
4 dimensions
claimed gap
```

并在 Web 结果页显示。

---

# 六十六、Scenario D — Provider Failure

Mock 使用：

```text
fallback / inconsistent / error mode
```

结果页应该：

```text
完成或带 warning
```

并明确：

```text
provider reliability issue
```

不能白屏。

---

# 六十七、Scenario E — Cancellation

开始 run。

执行中点击：

```text
Cancel
```

最终：

```text
Cancelled
```

停止后续请求。

刷新页面：

历史中仍能看到：

```text
Cancelled run
```

---

# 六十八、Scenario F — Restart

完成一个 run。

关闭 server。

重新：

```bash
llmtrace web
```

History 仍然存在。

这验证 SQLite。

---

# 六十九、测试策略发生变化

这一轮不要追求：

```text
每一个新 class 先写 20 个测试
```

优先：

# Vertical integration tests

核心测试：

```text
RunService
API
SQLite
SSE
Web result serialization
```

---

# 七十、最低测试

至少新增：

```text
test_run_service.py
test_storage.py
test_web_api.py
test_web_e2e.py
```

名字按项目实际调整。

---

# 七十一、FastAPI 测试

用：

```text
TestClient / httpx
```

验证：

```text
create
estimate
start
progress
result
history
cancel
```

---

# 七十二、前端不要做像素级测试

不需要：

```text
Playwright screenshot baseline
```

如果 Agent 环境已有浏览器工具：

做一次人工 smoke test。

没有就：

```text
HTML response + API e2e
```

够了。

---

# 七十三、完成定义

本轮只有一个 Definition of Done：

> **一个不知道 LLMTrace 内部架构的人，是否能够安装程序、打开网页、填写自己的第三方 API、点击开始，然后得到一份看得懂的验货结果？**

如果还需要：

```text
手写 Python
手动调用 5 个 CLI
自己找 Artifact
自己读 JSON
```

就没有完成。

---

# 七十四、禁止继续“修大门”

本轮尤其禁止：

```text
重构 ReferenceSet
重写 Calibration
重写 CapabilityProfile
重新设计 Suite
重新设计 Evidence
新的抽象基类体系
新的 Registry framework
新的 plugin framework
新的 repository framework
```

除非：

```text
不改就无法完成用户主流程
```

---

# 七十五、遇到旧设计不完美怎么办

例如发现：

```text
UnifiedAuditRunner progress 不够优雅
```

不要：

```text
重写 runner
```

加：

```text
callback
```

继续。

---

# 七十六、遇到 Confidence 理论不够完善怎么办

先：

```text
High / Medium / Low
```

继续。

---

# 七十七、遇到 Similarity 算法不够先进怎么办

先：

```text
simple normalized distance
```

并：

```text
Experimental
```

继续。

---

# 七十八、遇到 Routing 数据不足怎么办

显示：

```text
Insufficient Data
```

继续。

---

# 七十九、遇到前端不够漂亮怎么办

只要：

```text
干净
可读
不卡
响应式基本可用
```

继续。

以后装修。

---

# 八十、不要在中途停下来等 Owner

本轮：

```text
不要每做一个模块就停下来汇报
```

你可以阶段提交。

但必须连续完成：

```text
Service
→ Storage
→ Web
→ Progress
→ Result
→ History
→ Experimental analysis
→ E2E
```

然后一次性交付。

---

# 八十一、推荐实施顺序

这只是施工顺序，不是独立里程碑。

### Step 1

扫描：

```text
UnifiedAuditRunner
Execution models
RunArtifactRepository
CLI
reports
Behavior Drift
Calibration
```

只理解怎么接。

不要做长研究报告。

---

### Step 2

实现：

```text
SQLite RunIndex
```

---

### Step 3

实现：

```text
RunService
```

---

### Step 4

给：

```text
UnifiedAuditRunner
```

最小增加：

```text
progress callback
cancellation token
```

---

### Step 5

实现：

```text
FastAPI
```

---

### Step 6

实现：

```text
Jinja + JS
```

---

### Step 7

接：

```text
estimate
start
SSE
cancel
result
history
```

---

### Step 8

结果页直接消费现有：

```text
CapabilityProfile
Calibration
ClaimedGap
Protocol Findings
Behavior Drift
Evidence
```

---

### Step 9

补：

```text
Confidence v1
Similarity experimental
Routing Stability experimental
```

只做最简单可解释版本。

---

### Step 10

做完整 E2E。

---

# 八十二、建议文件结构

不要机械照抄。

根据项目真实结构调整。

大致可以是：

```text
src/llmtrace/

service/
    models.py
    runs.py

storage/
    sqlite.py

web/
    app.py
    routes.py
    templates/
        base.html
        index.html
        run.html
        history.html
    static/
        app.css
        app.js

analysis/
    confidence.py
    similarity.py
    routing.py
```

如果已有相似目录：

直接复用。

---

# 八十三、依赖控制

尽量只新增：

```text
fastapi
uvicorn
```

SQLite：

标准库。

Frontend：

已有 Jinja2 + 浏览器原生 JS。

不要再新增十几个包。

---

# 八十四、性能

这是本地工具。

第一版：

```text
1–3 个并发 run
```

就够。

不用设计高并发。

甚至可以：

```text
默认只允许一个 active run
```

如果这样能显著减少复杂度。

但 API 最好仍以 run_id 设计。

---

# 八十五、应用数据目录

建议：

```text
~/.llmtrace/
```

结构：

```text
llmtrace.db

runs/
references/
logs/
```

如果当前 Artifact Repository 已有明确 root：

与其兼容。

不要迁移历史工件。

---

# 八十六、应用首页历史

首页下方直接显示最近：

```text
5 runs
```

让用户打开应用就能看到：

```text
上一次测试
```

---

# 八十七、README

最终 README 开头就要写：

# Quick Start

```bash
git clone ...
cd LLMTrace

python -m venv .venv
source .venv/bin/activate

pip install -e .

llmtrace web
```

然后：

```text
Open http://127.0.0.1:8765
```

---

# 八十八、不要把开发文档放在 Quick Start 前面

用户第一眼应该知道：

> 怎么用。

而不是先看：

```text
Architecture
Reference Calibration Methodology
Integrity Model
```

这些放后面。

---

# 八十九、CLI Quick Start 也保留

高级用户：

```bash
llmtrace run ...
```

继续保留说明。

---

# 九十、Roadmap 改写

完成后：

不要继续把 Roadmap 写成：

```text
v0.5 Fingerprint
v0.6 Web
```

改为：

```text
Usable MVP — Complete
```

然后未来：

```text
MVP Hardening
Reference Data Expansion
Advanced Fingerprinting
Routing Statistics
Benchmark Expansion
Web Polish
```

也就是说：

> 先产品化，再精修。

---

# 九十一、不要为了更新 Roadmap 再写十页文档

README：

更新。

Roadmap：

简单更新。

Architecture：

只在有必要时加。

本轮目标是代码。

---

# 九十二、Git 提交

建议阶段提交：

```text
feat: add local run service and persistent run index

feat: add local web application and live run progress

feat: add usable result and history experience

feat: add first-pass confidence similarity and routing summaries

test: add usable MVP end-to-end coverage

docs: document local MVP quick start
```

数量不用严格一致。

---

# 九十三、不要中途 push 半成品让 Owner 验收

可以本地 commit。

全部主流程跑通后：

统一：

```text
push
```

然后建 PR。

---

# 九十四、质量门禁

最后仍然执行：

```text
ruff
mypy
pytest
coverage
```

但这里强调：

> **门禁通过是必要条件，不是产品完成条件。**

更重要的是：

```text
Web E2E
```

---

# 九十五、最终必须实际启动程序

最后必须真实运行：

```bash
llmtrace web
```

确认：

```text
server starts
```

然后至少通过：

```text
httpx
TestClient
curl
```

完整测试 API。

如果有浏览器工具：

实际打开页面。

---

# 九十六、最终必须完成 Mock E2E

使用现有 Mock Provider。

完整跑：

```text
Web form
→ create
→ estimate
→ start
→ SSE
→ runner
→ report
→ history
```

不能只测 route 返回 200。

---

# 九十七、最终报告不要长篇讲架构

最后汇报：

## Product

```text
能不能用：
YES / NO
```

## Start

```bash
llmtrace web
```

## User Flow

```text
input
→ estimate
→ start
→ progress
→ result
→ history
```

## Working Features

列用户能操作的东西。

## Experimental

列：

```text
confidence
similarity
routing
```

## Deferred

只列：

```text
真正不影响第一版使用的装修项目
```

## Gates

```text
ruff
mypy
pytest
coverage
E2E
```

## Git

```text
branch
HEAD
remote
PR
```

---

# 九十八、最终验收示例

最终用户应该能够看到类似：

```text
LLMTrace Audit Result

Endpoint
api.example.com

Claimed Model
model-x

Capability
78.4 / 100

Confidence
High

Reasoning
81.2

Coding
74.6

Math & Science
79.8

Instruction Following
76.1

Claimed Model Reference
86.7

Capability Gap
-8.3

Behavior Similarity   Experimental
Model A      82%
Model B      71%
Model C      64%

Routing Stability     Experimental
Mostly Stable

Protocol Risk
LOW

Performance
Requests       38
Input Tokens   24,381
Output Tokens   8,241
Average latency 2.4 s
P95 latency     4.9 s
Estimated cost $0.37

Previous Run
Overall capability -3.2
Reasoning          -7.1

[ Open Technical Report ]
[ Download JSON ]
```

---

# 九十九、最重要的判断标准

这轮不要再问：

> 这个模块是不是已经设计到未来五年都不用改？

只问：

> **现在普通用户能不能用了？**

如果：

```text
YES
```

先交付。

后续再：

```text
修算法
扩参考数据
上 IRT
增强 Fingerprint
增强 Routing
增加 benchmark
换 React
完善 UI
完善统计
```

---

# 一百、最后重申

从现在开始：

```text
Correct enough
+
Complete enough
+
Usable
```

优先于：

```text
Perfect
+
Incomplete
```

不要再花一整轮时间把某个已经能工作的基础模块从 90 分修到 97 分，却让整个产品仍然只能通过 CLI 和 JSON 使用。

**本轮必须把“整个小区”先建出来。**

现在从真实仓库开始工作。

连续施工到：

```text
llmtrace web
```

真正可用为止。

中途不要因为非 Blocker 问题停止。

不要合并 main；完成后推送 feature branch 并创建 PR，交给 Owner 验收。